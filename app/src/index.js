import React from 'react';
import ReactDOM from 'react-dom';
import { BrowserRouter as Router, Switch, Route, Link } from "react-router-dom";
import Inicio from './features/index/index';

ReactDOM.render(
  <Inicio />,
  document.getElementById('root')
);